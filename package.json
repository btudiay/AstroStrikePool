{
  "name": "astro-strike-pool",
  "version": "1.0.0",
  "description": "Privacy-preserving prediction pool with FHE encryption",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.cjs --network sepolia",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@fhevm/hardhat-plugin": "0.3.0-0",
    "@fhevm/solidity": "^0.9.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.26.3"
  }
}
